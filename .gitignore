# ──────────────────────────────────────────────────────────────
# Stitchy London — PUBLIC repo (GitHub + Netlify + jsDelivr CDN)
#
# Default: everything is committed so Netlify can deploy the
# prototype as a static site. Internal docs, briefs and local
# tooling are kept out.
# ──────────────────────────────────────────────────────────────

# OS / editor cruft
.DS_Store
.vscode/

# Local Claude Code config
.claude/

# Internal documentation (reference only, not for public repo)
docs/
CLAUDE.md
PROJECT_BRIEF.md
PROJECT_PROGRESS.md
HANDOVER.md
